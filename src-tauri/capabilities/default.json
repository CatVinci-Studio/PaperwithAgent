{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Verko (stage 1 PoC).",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
